@model IEnumerable<string>
<div class="card">
    <div class="card-header">NHÀ SX</div>
    <div class="list-group">
        @foreach (var item in Model)
        {
            <a class="list-group-item">@item</a>
        }
    </div>
</div>